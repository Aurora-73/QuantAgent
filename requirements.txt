# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
duckdb>=1.0
pyyaml>=6.0
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0

# Data (A股)
akshare>=1.15

# Research
vectorbt>=0.26
riskfolio-lib>=6.0

# LLM
openai>=1.60
langchain>=0.3
langgraph>=0.4
langchain-openai>=0.3
langgraph-checkpoint-sqlite>=1.0

# Monitoring
rich>=13.0
schedule>=1.2
loguru>=0.7

# ============================================================
# 以下需要额外安装，详见文档
# ============================================================

# Qlib (需要编译 C 扩展)
# pip install qlib  (推荐从 PyPI 安装预编译版本)
# 或: cd ../_reference/qlib && pip install -e .

# vnpy (需要 TA-Lib C 库)
# 先安装 TA-Lib: sudo apt install libta-lib-dev
# 然后: pip install ta-lib vnpy vnpy-ctp vnpy-ib

# OpenBB (依赖较多)
# pip install openbb (从 PyPI 安装)
